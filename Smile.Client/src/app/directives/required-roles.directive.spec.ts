import { RequiredRolesDirective } from './required-roles.directive';

describe('RequiredRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredRolesDirective();
    expect(directive).toBeTruthy();
  });
});
